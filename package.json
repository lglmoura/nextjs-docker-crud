{
  "name": "nextjs-docker-crud",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev":"npm install --force",
    "dev": "dotenv -e .env -- prisma migrate reset --force && next dev",
    "build": "next build",
    "start": "next start",
    "start:prod": "node dist/main",
    "dev:docker:down": "docker-compose --env-file ./.env -f docker-compose.yml  down",
    "dev:docker:up": "docker-compose --env-file ./.env -f docker-compose.yml  up -d --build",
    "dev:docker:start": "npm rum dev:docker:down && npm rum dev:docker:up   && sleep 1",
    "prestartmigr:dev": "npm rum dev:docker:start && dotenv -e .env -- prisma migrate reset --force",
    "seed:dev": "dotenv -e .env -- prisma db seed ",
    "prestart:dev": "npm rum dev:docker:start",
    "start:dev": "dotenv -e .env -- next dev",
    "migrate:dev": "dotenv -e .env -- npx prisma migrate dev",
    "migrate:dev:create": "dotenv -e .env -- npx prisma migrate dev --create-only",
    "migrate:dev:reset": "dotenv -e .env -- npx prisma migrate reset --force",
    "migrate:dev:resolve": "dotenv -e .env -- npx prisma migrate resolve",
    "prisma:dev:studio": "dotenv -e .env -- npx prisma studio",
    "prisma:dev:generate": "dotenv -e .env -- npx prisma generate",
    "start:docker": "dotenv -e .env -- next dev",
    "migrate:docker": "dotenv -e .env -- npx prisma migrate dev",
    "lint": "next lint"
  },
  "dependencies": {
    "@prisma/client": "^4.16.1",
    "@types/node": "20.3.1",
    "@types/react": "18.2.14",
    "@types/react-dom": "18.2.6",
    "prisma": "^4.16.1",
    "autoprefixer": "10.4.14",
    "axios": "^1.4.0",
    "daisyui": "^3.1.6",
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.2.1",
    "eslint": "8.43.0",
    "eslint-config-next": "^12.0.4",
    "next": "13.4.7",
    "postcss": "8.4.24",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "tailwindcss": "3.3.2",
    "typescript": "5.1.3"
  },
  "devDependencies": {
    
  }
}
